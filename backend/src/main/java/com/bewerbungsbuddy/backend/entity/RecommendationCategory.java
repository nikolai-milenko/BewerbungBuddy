package com.bewerbungsbuddy.backend.entity;

public enum RecommendationCategory {
    ADD_KEYWORDS,
    REPHRASE_EXPERIENCE,
    HIGHLIGHT_ACHIEVEMENTS,
    FORMAT_IMPROVEMENT,
    GRAMMAR_STYLE
    // die Liste kann beim Bedarf noch ergänzt werden
}
